version: '3.8'

services:
  uno:
    build: .
    container_name: uno-multiplayer
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - PORT=3000
      - NODE_ENV=production
